# Loaded by `next dev` only (never by `next build`). In production the homepage
# and app share the scriptio.app origin, so app links stay relative (empty here).
# In local dev the app runs on a separate port, so point cross-origin links at it.
NEXT_PUBLIC_APP_ORIGIN=http://localhost:3000
