import EditorAndSidebar from "./EditorAndSidebar";

const EditorContainer = () => {
  return (
    <div id="editor-page">
      <EditorAndSidebar />
    </div>
  );
};

export default EditorContainer;
